# V1.6.2 — Clean First Run

Each released Synopsis Browser version uses a version-specific profile under LocalAppData.

Fresh profile data includes:

- no linked projects
- no saved Ollama model
- no API key
- no recent URLs
- no bookmarks
- no previous diagnostics or AI reviews

The same version keeps its settings between launches. Older version profiles are not imported automatically.
